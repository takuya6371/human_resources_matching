-- ============================================================
-- 未使用スキーマの削除
--
-- contact_requests: 「特定の人材へ直接問い合わせ」機能用に用意されて
-- いたが、フロントエンドから一度も参照されないまま。実際の問い合わせは
-- inquiries テーブル（汎用フォーム）のみで運用されている。
--
-- marital_status: 使い道のないまま追加されていたプロフィール項目。
-- 婚姻状況は採用選考で扱うべきでない機微情報であり、
-- 使われていないなら持たせておく理由がない。
-- ============================================================
drop table if exists public.contact_requests;

alter table public.profiles drop column if exists marital_status;
